{% extends 'base.html.twig' %}

{% block body %}
        <div class="panel panel-info"   >
        <div class="panel-heading">
            <h1>{{ blog.title }}</h1>
            <p>{{ blog.createdAt | date("Y-m-d H:i") }}</p>

        </div>
        <div class="panel-body">
            <p>{{ blog.content }}</p>
        </div>
    </div>
{% endblock %}

